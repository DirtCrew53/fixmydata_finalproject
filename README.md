# fixmydata - A Python Library for Data Cleaning

## Overview
**fixmydata** is a Python library designed to clean and preprocess data. It implements common data cleaning tasks such as handling missing values, removing duplicates, detecting outliers, and validating dataset quality using Object-Oriented Programming (OOP) principles.

## Installation
To install the library, use pip:

```bash
pip install fixmydata
